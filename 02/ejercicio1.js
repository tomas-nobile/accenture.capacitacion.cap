/*============================================
ANCHOR Ejercicio 1 - Cargar de forma automática incremental, 
un array de números a partir de un largo dado por el usuario.
=============================================*/

const len = 2;
const arr = [];

for (let i = 0; i < len; i++) {
  arr.push(i);
}

console.log(arr);
